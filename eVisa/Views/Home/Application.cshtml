@{
    ViewBag.Title = "Application Form";
}
<div class="content">
    <div class="container">
        <div class="row bs-wizard" style="border-bottom:0;">
            <div class="col-xs-3 bs-wizard-step complete">
                <div class="text-center bs-wizard-stepnum">Apply</div>
                <div class="progress"><div class="progress-bar"></div></div>
                <a href="#" class="bs-wizard-dot"></a>
            </div>

            <div class="col-xs-3 bs-wizard-step complete">
                <!-- complete -->
                <div class="text-center bs-wizard-stepnum">Application</div>
                <div class="progress"><div class="progress-bar"></div></div>
                <a href="#" class="bs-wizard-dot"></a>
            </div>

            <div class="col-xs-3 bs-wizard-step">
                <!-- complete -->
                <div class="text-center bs-wizard-stepnum">Review</div>
                <div class="progress"><div class="progress-bar"></div></div>
                <a href="#" class="bs-wizard-dot"></a>
            </div>

            <div class="col-xs-3 bs-wizard-step">
                <!-- active -->
                <div class="text-center bs-wizard-stepnum">Payment</div>
                <div class="progress"><div class="progress-bar"></div></div>
                <a href="#" class="bs-wizard-dot"></a>
            </div>
        </div>
        <h1>Application Form</h1>
        <div class="appSubsTitle">You can apply up to 9 applicants. (All fields must fill up only English character is required.)</div>
        <br />
        @*using (Html.BeginForm("SaveApplication", "Home", null, FormMethod.Post, new { @id = "myForm", @name = "myForm" }))
            {*@

        <form name="appForm">
            @Html.Partial("~/Views/Home/ApplicationFormPartial.cshtml")
            <br />
            <button class="btn" data-ng-click="backApplication();"><i class="fa fa-backward"></i> Back</button>
            <button class="btn" data-ng-click="nextApplication();"><i class="fa fa-save"></i> Save & Continue</button>
            <br /><br />
        </form>
            @*}*@
</div>
</div>